class MealData {
  MealData(this.mealDescription, this.mealDate, this.mealImageUrl, this.docId,
      this.localPath);
  String mealDescription = "";
  DateTime mealDate;
  String mealImageUrl;
  String docId;
  String localPath;
}
